<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>This is the index.</h1>
      <% for (let i = 0; i < beers.length; i++) { %>
        <div>
          <h3><%=beers[i].name%></h3>
          <h4><%=beers[i].company%></h4>
          <!-- <p><%=beers[i].description%></p> -->
          <img src="<%=beers[i].img%>" alt="<%=beers[i].name%>">
          <h5><%=beers[i].rating%></h5>
          <a href="/The-Beer-Cellar/<%=beers[i].id%>">Show Details</a>
          <form action="/The-Beer-Cellar/<%=beers[i].id%>?_method=DELETE" method="POST">
            <input type="submit" value="Delete Entry">
          </form>
        </div>
      <%
      }
      %>
      <a href="/The-Beer-Cellar/New">New Entry</a>
  </body>
</html>
